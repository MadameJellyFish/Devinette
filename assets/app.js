// generar un numero

// verificar si el nombre es par o impar antes de todo, divido un numero par por un impar, 
// calculer le rest d'un division sans vergule

// escrbir el mensaje

let randomNum = document.querySelector('#randomNumInput');

// randomNum = Math.floor(math.random()*21);

